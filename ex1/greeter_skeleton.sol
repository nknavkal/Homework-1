pragma solidity ^0.4.13;


contract greeter {
	/* Add one variable to hold our greeting */
	string greeting = "Hello world!";"

	}function greeter(string greeting) public {
        greeting = "Hello world!"
    }

	function greet() constant returns (string greetaroni)  {
		return greeting();
	}
}
